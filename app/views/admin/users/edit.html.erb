<%= form_with(model: @user, url: admin_user_path, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= form.label :email %>
    <%= form.email_field :email, class: 'form-control' %>
  </div> 
  <div class="form-group">
    パスワードを変更する場合は新しいパスワードを6文字以上で入力してください(変更しない場合は空欄)<br>
    <%= form.password_field :password, class: 'form-control' %>
  </div>
  <div class="form-group">
    新しいパスワード入力確認<br>
    <%= form.password_field :password_confirmation, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= form.label t("view.admin_auth") %>
    <%= form.select :admin, User.admins.keys.map {|k| [I18n.t("enums.user.admin.#{k}"), k]}, class: 'form-control' %></td>
  </div>
  <div class="form-group">
    <%= form.submit %>
  </div>
<% end %>
